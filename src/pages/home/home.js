import { Button, Container, VStack } from "@chakra-ui/react";

function Home() {
    return(
        <Container>
            <VStack>
                <Button>Hello!</Button>
            </VStack>
        </Container>
    );
}

export default Home;